{
  "scripts": {
    "install": "cd frontend && pnpm install && cd ../backend && bun install",
    "dev": "concurrently \"cd frontend && pnpm run dev\" \"cd backend && bun dev\"",
    "dev:frontend": "cd frontend && pnpm run dev",
    "dev:backend": "cd backend && bun dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
